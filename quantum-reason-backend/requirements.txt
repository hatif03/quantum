# Quantum Reason Backend Requirements

openai>=1.40.0
pydantic>=2.7.0
python-dotenv>=1.0.0
numpy>=1.24.0

httpx>=0.27.0

fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Optional — vector KB search embeddings (keyword fallback without key)
google-generativeai>=0.8.0

# Optional — vector KB search (falls back to keyword search without it)
# annoy>=1.17.0

typing-extensions>=4.11.0
